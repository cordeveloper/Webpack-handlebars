// evax-modal EJEMPLO DE WEBCOMPONENT NATIVO
/* import './components/evax-modal.js';  */
// Components


(() => {
  window.onload = () => {

    /* const modal = document.querySelector('evax-modal');

    if(modal) {
     
      if (!window.customElements ) { 
        const modalText = modal.textContent;
       } else {
         setTimeout(() => {
           modal.open();
         }, 1000)
       }

    }  */
  
  }
})();